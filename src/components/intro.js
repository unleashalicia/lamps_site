import React from 'react';

const Intro = props => {
    const introStyle = {
        width: "100vw",
        height: "300px",
        backgroundColor: "#385149"
    };

    return (
      <div style={introStyle}>

      </div>
    );
};

export default Intro;